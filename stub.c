#include "FarenheitToCelsius.h"
int stub(celcius)
{
    // Return 200 for ok
    // Return 500 for not-ok
    if(celcius > 0)
    {
        return 200;
    }
    else
    {
        return 500;
    }
}
